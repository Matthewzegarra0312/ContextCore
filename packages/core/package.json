{
  "name": "@contextcore/core",
  "version": "0.1.0",
  "description": "Motor de ContextCore: detección de stack, storage append-only, compilador multi-formato (AGENTS.md/CLAUDE.md/.cursor/rules). Usado por el paquete `contextcore` (CLI).",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Matthewzegarra0312/ContextCore",
    "directory": "packages/core"
  },
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "node-llama-cpp": "^3.19.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.11",
    "typescript": "^5.5.4"
  }
}
