{
  "name": "contextcore-monorepo",
  "private": true,
  "version": "0.0.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r --filter=./packages/** build",
    "dev:cli": "pnpm --filter @contextcore/cli dev",
    "dev:web": "pnpm --filter web dev"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
